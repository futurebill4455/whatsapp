{
  "command": "node -e \"require('./src/config/db'); const {Settings}=require('./src/models'); const prev=Settings.get('anti_ban_hours_enabled'); Settings.set('anti_ban_hours_enabled','0'); const {evaluateAccessGate}=require('./src/middleware/bot'); const {AccessUsers}=require('./src/models'); const phone='9561777003'; const code='MID'+Date.now().toString(36).toUpperCase().slice(-5); for(const u of AccessUsers.list()){if(String(u.phone).includes('9561777003')||u.name==='Middleware Test')try{AccessUsers.remove(u.id)}catch(_){}} const u=AccessUsers.create({name:'Middleware Test',phone,access_code:code}); console.log('unauth', evaluateAccessGate({phone:'999',body:code})); console.log('wrong', evaluateAccessGate({phone,body:'WRONG99'})); console.log('ok', evaluateAccessGate({phone:'+91'+phone,body:code})); AccessUsers.remove(u.id); if(prev!=null) Settings.set('anti_ban_hours_enabled', prev); else Settings.set('anti_ban_hours_enabled','1'); const {buildCustomerCloseNotice}=require('./src/middleware/bot'); console.log('closeNotice', buildCustomerCloseNotice({id:1,session_code:'AB12',customer_phone:'9199',company_name:'Star'}));\"",
  "description": "Smoke access gate with hours disabled"
}