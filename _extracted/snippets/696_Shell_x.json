{
  "command": "node --check src/services/whatsapp.js; node --check src/services/deskForward.js; node --check src/middleware/bot/accessGate.js; node --check src/middleware/bot/deskRouter.js; node --check src/middleware/bot/sessionClose.js; node --check src/middleware/bot/index.js; node -e \"require('./src/config/db'); const {evaluateAccessGate,resolveCompanyDesk}=require('./src/middleware/bot'); const {AccessUsers,Companies}=require('./src/models'); const phone='9561777003'; const code='MID'+Date.now().toString(36).toUpperCase().slice(-5); for(const u of AccessUsers.list()){if(String(u.phone).includes('9561777003')||u.name==='Middleware Test')try{AccessUsers.remove(u.id)}catch(_){}} const u=AccessUsers.create({name:'Middleware Test',phone,access_code:code}); console.log('unauth', evaluateAccessGate({phone:'999',body:code})); console.log('wrong', evaluateAccessGate({phone,body:'WRONG99'})); console.log('ok', evaluateAccessGate({phone:'+91'+phone,body:code})); AccessUsers.remove(u.id); const cos=Companies.list(true); const sample=cos.find(c=>c.desk_phone)||cos[0]; if(sample){ console.log('deskRouter', resolveCompanyDesk({company:sample.name,insurance_type:'Health'})); } console.log('missing', resolveCompanyDesk({company:'__no_such__'})); require('./src/services/whatsapp'); console.log('load OK');\"",
  "description": "Syntax-check and smoke middleware modules"
}